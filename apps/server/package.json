{
  "name": "@kotoba-gym/server",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wrangler d1 migrations apply kotoba-gym --local && wrangler dev --local --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy --minify",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src migrations package.json tsconfig.json wrangler.jsonc",
    "format": "biome format --write src migrations package.json tsconfig.json wrangler.jsonc",
    "cf:typegen": "wrangler types"
  },
  "dependencies": {
    "@kotoba-gym/core": "workspace:*",
    "hono": "^4.12.15"
  },
  "devDependencies": {
    "typescript": "^5",
    "wrangler": "^4.4.0"
  }
}
