{
  "name": "@kotoba-gym/core",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "react-native": "./react-native.ts",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check src react-native.ts personalization.react-native.ts",
    "format": "biome format --write src react-native.ts personalization.react-native.ts"
  },
  "dependencies": {
    "zod": "^3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1",
    "typescript": "^5"
  }
}
